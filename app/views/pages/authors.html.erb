<h1  class="page_title">Authors</h1>
<hr>

<% @authors.each do |auth| %>
  <div><%= auth.name %></div>
  <div><%= "Blogs: #{auth.blog.count}" %></div>
  <div>
    <% auth.blog.each do |blog| %>
      <div><%= link_to blog.title, blog %></div>
    <% end %>
  </div>
<% end %>